// import { schema } from "@acme/db";

// import { createTRPCRouter, publicProcedure } from "../trpc";

// export const categoryRouter = createTRPCRouter({
//   all: publicProcedure.query(({ ctx }) => {
//     return ctx.db.select().from(schema.categories);
//   }),
// });
